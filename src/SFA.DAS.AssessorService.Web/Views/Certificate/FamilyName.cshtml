@using SFA.DAS.AssessorService.Web.ViewModels.Certificate
@model CertificateNamesViewModel

@{
    ViewBag.Title = (!ViewData.ModelState.IsValid ? "Error: " : "") + "Apprentice's last name";
    Layout = "_DashboardLayout";
}

<div class="govuk-width-container">
    <main class="govuk-main-wrapper " id="main-content" role="main">
        <a class="govuk-back-link" asp-controller="CertificateCheck" asp-action="Check">Back</a>
        <div class="govuk-grid-row">
            <div class="govuk-grid-column-two-thirds">

                <form class="apprentice-name-form" method="post" asp-controller="CertificateFamilyName" asp-action="FamilyName">

                    <input type="hidden" name="Id" value="@Model.Id" />
                    <input type="hidden" name="GivenNames" value="@Model.GivenNames" />
                    <input type="hidden" name="FamilyName" value="@Model.FamilyName" />
                    <input type="hidden" name="InputGivenNames" value="@Model.GivenNames" />

                    @if (!ViewData.ModelState.IsValid)
                    {
                        <div class="govuk-error-summary" aria-labelledby="error-summary-title" role="alert" tabindex="-1" data-module="govuk-error-summary">
                            <h2 class="govuk-error-summary__title" id="error-summary-title">
                                There is a problem
                            </h2>
                            <div class="govuk-error-summary__body">
                                <ul class="govuk-list govuk-error-summary__list">
                                    <li><a href="#InputFamilyName" sfa-validation-for="InputFamilyName"></a></li>
                                </ul>
                            </div>
                        </div>
                    }

                    <h1 class="govuk-heading-xl">Apprentice's last name</h1>
                    <p class="govuk-body">You can edit the apprentice's last or family name if the letter case is incorrect. <br />For example you can change 'smith' to 'Smith' so that it begins with a capital letter.</p>
                    <p class="govuk-body">You cannot change the spelling or add more names. You'll need to contact the training provider to do this.</p>
                    <p class="govuk-body">Once you've saved the changes, that's how the apprentice's name will appear on their printed certificate.</p>

                    <div class="govuk-form-group" sfa-anyvalidationerror-class="govuk-form-group--error">
                        <label class="govuk-label govuk-label--m" for="InputFamilyName">
                            What is the apprentice's last name?
                        </label>
                        <span class="govuk-visually-hidden">Error:</span>
                        <span class="govuk-error-message" asp-validation-for="InputFamilyName"></span>
                        <input class="govuk-input" id="InputFamilyName" asp-for="InputFamilyName" type="text" spellcheck="false" autocomplete="family-name" sfa-validation-for="InputFamilyName" sfa-validationerror-class="govuk-input--error" />
                    </div>

                    <button type="submit" class="govuk-button" data-module="govuk-button">
                        Save and continue
                    </button>

                </form>
            </div>
        </div>
    </main>
</div>

