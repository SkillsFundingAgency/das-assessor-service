@using Microsoft.AspNetCore.Mvc.Localization
@inject IViewLocalizer Localizer
@model SFA.DAS.AssessorService.Web.ViewModels.Search.SearchViewModel

@{
    ViewBag.Title = "Confirm apprentice";
    Layout = "_Layout";
}


<div class="grid-row">
    <div class="column-two-thirds">
        <a class="link-back" asp-controller="Search" asp-action="Index">Back</a>
        @*@if(Model.SearchResults.Count() == 1) {*@
        <h1 class="heading-large">Confirm apprentice</h1>
        <div class="form-group">
            <fieldset>
                <ul>
                    <li>
                        <p><span class="bold">Name:</span> @Model.SearchResults.First().GivenNames @Model.SearchResults.First().FamilyName</p>
                    </li>
                    <li>
                        <p><span class="bold">ULN:</span> @Model.SearchResults.First().Uln</p>
                    </li>
                    <li>
                        <p><span class="bold">Standard:</span> @Model.SearchResults.First().Standard</p>
                    </li>
                </ul>
            </fieldset>
        </div>
        @*}
            else
            {
                <h1 class="heading-large">The apprentice is enrolled on more than one apprenticeship</h1>
                <p>Select the standard that you would like to record an assessment for.</p>
                <div class="form-group">
                    <fieldset>
                        <ul>
                            <li>
                                <p><span class="bold">Name:</span> @Model.SearchResults.First().GivenNames @Model.SearchResults.First().FamilyName</p>
                            </li>
                            <li>
                                <p><span class="bold">ULN:</span> @Model.SearchResults.First().Uln</p>
                            </li>
                        </ul>
                        @foreach (var result in Model.SearchResults)
                        {
                            <div class="multiple-choice">
                                <input id=@("standard_" + result.StdCode) type="radio" name="radio-group" value="@result.StdCode">
                                <label for=@("standard_" + result.StdCode)>@result.Standard</label>
                            </div>
                        }
                    </fieldset>
                </div>
            }*@
        <form asp-controller="Certificate" asp-action="Start" method="post">
            @Html.HiddenFor(m => m.Uln)
            @Html.HiddenFor(m => m.SearchResults.First().StdCode)
            <div class="form-group" style="margin-top: 25px;">
                <button class="button button-white" type="submit">Start recording assessment</button>
            </div>
        </form>
    </div>
</div>

