@using Microsoft.AspNetCore.Mvc.Localization
@inject IViewLocalizer Localizer
@model SFA.DAS.AssessorService.Web.ViewModels.Search.SearchViewModel

@{
    ViewBag.Title = (ViewData.ModelState.IsValid ? "" : "Error: ") + "Search for an apprentice";
    Layout = "_Layout";
}

<div class="grid-row">
    <div class="column-two-thirds">
        <h1 class="heading-large">Search for an apprentice</h1>
        <div class="info-summary">
            <h3 class="heading-medium">Only record successful assessments</h3>
            <p>You don't need to record failed assessments. Please search for the apprentice again once they have passed.</p>
        </div>


        <form method="post">
            @if (!ViewData.ModelState.IsValid)
            {
                <div class="error-summary" role="alert" aria-labelledby="error-summary-heading" tabindex="-1">
                    <h2 class="heading-medium error-summary-heading" id="error-summary-heading">
                        Message to alert the user to a problem goes here
                    </h2>
                    <p>
                        Optional description of the errors and how to correct them
                    </p>
                    <ul class="error-summary-list">
                        <li>
                        <a href="#example-name">Descriptive link to the question with an error</a>
                        </li>
                    </ul>
                </div>
            }
            <fieldset>
                <div class="form-group">
                    <label class="form-label" for="Uln">@Localizer["Uln"]</label>
                    <input class="form-control" asp-for="Uln" type="number" /> <span asp-validation-for="Uln"></span>
                    @*<label class="form-label" for="unique-learner-number">Unique learner number (ULN)</label>
                    <input class="form-control" id="unique-learner-number" type="number" name="unique-learner-number">*@
                </div>
                <div class="form-group">
                    <label class="form-label" for="surname">@Localizer["Lastname"]</label>
                    <input class="form-control" asp-for="Surname" type="text" /> <span asp-validation-for="Surname"></span>
                    @*<label class="form-label" for="learner-last-name">Last name</label>
                    <input class="form-control" id="learner-last-name" type="text" name="learner-last-name">*@
                </div>
                <details role="group">
                    <summary role="button" aria-controls="what-is-a-unique-learner-number" aria-expanded="false"><span class="summary">What is a unique learner number (ULN)?</span></summary>
                    <div class="text" id="what-is-a-unique-learner-number" aria-hidden="true">
                        <p>
                            Each apprentice has a unique learner number (ULN) that is used to identify them. It is ten digits long and contains only numbers.
                        </p>
                    </div>
                </details>
                <div class="form-group">
                    <input class="button" id="button-search" type="submit" value="Continue">
                </div>
            </fieldset>
            
        </form>
    </div>
</div>

@if (Model?.SearchResults != null)
{
    if (!Model.SearchResults.Any())
    {
        <p>No results</p>
    }
}